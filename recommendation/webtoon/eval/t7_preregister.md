# T-7 사전등록 — 웹툰 **작가 정합** `creator_w` (TMDB V-2 전이 검증) (2026-09-13)

## 왜 — 다른 플랫폼에서 해결한 문제가 여기에도 있는가
TMDB V-2 에서 **"임베딩에 창작자가 없어서 같은 감독 작품이 추천에 거의 안 나온다"**를 확인하고
감독 정합 항으로 고쳤다(director_w 0.10 · 역할 B +0.056 · 롱테일 ±0, 봉인 cd9687a9…).
같은 결손이 웹툰에도 있는지 채점 전에 기계적으로 쟀다(판정 아님).

| 진단 (프로덕션 설정, 67프로필) | 웹툰 | TMDB V-2 기준선 |
|---|---|---|
| 임베딩 텍스트에 작가명 포함 | **8.6%** (줄거리 홍보문구에 우연히) | 0% |
| 대상군 (시드 작가의 다른 작품이 코퍼스에 있음) | 64/67 | 50/52 |
| 시드작가 일치@10 | **0.54** | 0.31 |
| 같은 작가 상한 `artist_max=2` 가 top-10 을 바꾸는가 | 거의 아니다 (0.54 → 상한 끄면 0.55) | (상한 없음) |

**상한이 원인이 아니다.** 같은 작가 작품이 애초에 상위에 안 올라온다 — 임베딩이 작가를 모르기 때문이다.

## 가설 H-T7
> 시드와 같은 작가(글·그림·원작)의 작품에 랭킹 보너스를 주면 사용자가 실제로 누를 추천이 늘어난다.
> 보너스 보류(감점 없음) 구조라 저관심 프로필은 다치지 않는다. **V-2 의 결론이 웹툰으로 전이된다.**

반증 조건: 아래 판정 (C) — 웹툰 독자에게 같은 작가는 선호 신호가 아니거나(그림 작가만 같은 경우 등),
tag_w 0.2(T-3)가 이미 같은 결을 잡아 추가 이득이 없다.

## 수식 — 웹툰 랭커의 기존 모양(더하기 보정) 그대로
    creator_align(A) = max over seeds S_i of |작가(A) ∩ 작가(S_i)| / |작가(S_i)|
    final = sim × (1 + tag_w × tag_cov + creator_w × creator_align)
작가 집합 = `artists`(글·그림·원작 목록), 없으면 `author` 를 ' / ' 로 분리. 작가 없는 후보는 0(무보정).

## 격자 — 팔 3개 (V-2 격자를 그대로 이식한다: 전이를 검증하는 라운드이므로)
기계 진단(채점 없음):

| w | 작가일치@10 | 저관심 | top-10 한 작가 최다(최대) | 기준 겹침@50 | ILS@50 |
|---|---|---|---|---|---|
| **0.00** | 0.54 | 0.53 | 1.21 (2) | 100% | 0.522 |
| 0.05 | 0.93 | 1.06 | 1.27 (2) | 99% | 0.522 |
| **0.10** | 1.24 | 1.35 | 1.36 (3) | 98% | 0.522 |
| **0.25** | 2.39 | 2.29 | 1.69 (4) | 95% | 0.520 |
| 0.50 | 3.67 | 3.47 | 1.99 (4) | 92% | 0.517 |

- 0.05 제외: 기준과 99% 겹침 — §84 잡음 폭 아래 (V-2 와 같은 규칙).
- 0.50 제외: V-2 에서 필터화로 뺀 값이고, 여기서도 top-10 한 작가 최다가 기준의 1.6배(최대 4)다.
- w=0 목록이 프로덕션 목록과 67/67 **완전 일치**함을 확인했다(설정 기본값 누수 없음).

| 팔 | creator_w |
|---|---|
| **c0** | 0.00 |
| **c10** | 0.10 |
| **c25** | 0.25 |

다른 축 전부 `PRODUCTION` 고정: rep_v2 · top2_mean · pop 0 · hub 0 · star 0 · tag_w 0.2 · 후처리(series 1 · artist 2 · adult 제외).

## 프로필·표본·채점
프로필 67(전체 생성) · **주 판정은 대상군 64** · k=50 · 슬롯 1-10 중 5 + 11-50 중 5 · **RNG seed 7** · **미채점 0 전 비교 금지.**
**은행 재사용 없음** — 기존 T 은행은 다수결만 저장해 역할별 등급이 없다. 전부 새로 3역할 채점한다.
시트 meta 는 기존과 동일(장르·태그 6·회차 수). **작가·관심수·평점은 시트에 넣지 않는다** — 채점자가
"같은 작가라서" 점수를 주는 것이 아니라 작품의 결로 판단하게 한다(V-2 도 감독을 시트에 넣지 않았다).

**판정은 역할 B 단독**, 역할 A 는 저관심 안전 확인, 역할 C 는 부수 관측. 이유는 V-2 와 같다(§84: 인기 배제
지시를 받은 역할 A 는 인기 상관 0.063). 역할 프롬프트는 `eval/t1_preregister.md`(md5 2f06edaa5549fac17941f79dcebe85e4) 원문:

> **역할 A** — 당신은 추천 시스템의 품질 평가자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 웹툰 시드 목록(seed)과 시스템이 추천한 웹툰 후보(cand, meta)입니다.
> 각 후보가 그 시드를 좋아하는 사용자에게 얼마나 관련 있는 추천인지 등급을 매기세요: 3 = 강한 적합 (시드와 장르·테마·톤이 명확히 맞음), 2 = 적합 (같은 계열, 자연스러운 추천), 1 = 약함 (겹치는 요소가 하나뿐이거나 결이 다름), 0 = 무관. 기준은 오직 시드와의 관련성입니다. 인기·평점은 보지 마세요. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.
>
> **역할 B** — 당신은 웹툰을 매우 많이 보는 헤비 독자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 어떤 사용자가 좋아한 웹툰 시드 목록(seed)과 앱이 그 사용자에게 추천한 웹툰 후보(cand, meta)입니다.
> 당신이 그 시드들을 좋아하는 사람이라고 상상하고 "이 추천을 보면 실제로 눌러 볼까?"로 등급을 매기세요: 3 = 바로 볼 것 같다, 2 = 볼 만하다 (납득되는 추천), 1 = 굳이? (겹치는 게 하나뿐이거나 결이 다름), 0 = 왜 이게 나왔지. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.
>
> **역할 C** — 당신은 웹툰 플랫폼의 편집자/큐레이터입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 웹툰 시드 목록(seed)과 알고리즘이 추천한 웹툰 후보(cand, meta)입니다.
> "이 시드를 좋아하는 사용자에게 보여주는 추천 목록에 이 후보를 넣어도 편집자로서 부끄럽지 않은가"로 등급을 매기세요: 3 = 자신 있게 넣는다, 2 = 넣어도 된다, 1 = 넣기 애매하다 (겹치는 요소가 약하거나 톤·연령대 등 결이 다름), 0 = 빼야 한다. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 그 역할 등급 ≥ 2. 주 지표 = **역할 B 적합률 · 대상군 64 · k=50 합산.**
저관심 = 시드 관심수 중앙이 67프로필 중 하위 25% (= 17개, ≤ 28,706). 등급을 보지 않고 정의된다.

- **(A) 채택** — 셋 다 만족하는 w 중 Δ_B 최대:
  1. `Δ_B(w) ≥ +0.04`
  2. 단조성: c10 과 c25 **둘 다** c0 보다 높다
  3. 안전: 저관심 17 에서 역할 A `Δ_A ≥ −0.03`
- 추가 안전장치(웹툰 관례 T-3~T-6): (E) ILS@50 이 c0 보다 +0.03 이상 높으면 채택 안 함 ·
  (F) `mix` 12프로필 역할 B 가 c0 보다 0.05 이상 낮으면 채택 안 함
- **(B) 보류** — `+0.02 ≤ Δ_B < +0.04` 이거나 단조성만 깨짐 → 0 유지, 재현 필요 기록
- **(C) 기각** — 그 외 → 0 유지. 문턱을 낮춰 다시 재지 않는다
- 부수 관측: 3역할 다수결 · 역할 A/C 전체 · 67 전체 · ILS · 작가일치@10

## 예상 (관측 전)
**(B) 보류 또는 약한 (A), Δ_B +0.02 ~ +0.05.** V-2 보다 작을 것으로 본다. 이유 둘:
1. 기준선 작가일치가 이미 0.54 로 TMDB(0.31)보다 높다 — 메울 결손이 작다.
2. tag_w 0.2(T-3, +0.075) 가 이미 "편집된 취향 라벨"로 같은 작가 작품의 결을 일부 잡고 있다.
반대 방향 위험: 웹툰 작가 집합에 **원작자**가 섞여 있어(웹소설 원작 코미컬라이즈) 결이 다른 작품이 묶일 수 있다.
