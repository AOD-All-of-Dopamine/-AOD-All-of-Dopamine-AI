# T-10 사전등록 — 웹툰 **태그 피복의 장르명 포화** (`tag_drop_genre`) (2026-09-15)

## 왜 — X-26 (C) 붕괴의 원인 진단에서 나왔다
X-26(md5 4facba27…)에서 웹툰 새로고침 제품 경로가 1페이지 0.757 → 5페이지 **0.643** 으로 무너졌다(사전등록 문턱 F5 < 0.70).
진단(`eval/x26_verdict.md`):
- 코퍼스 고갈이 아니다 — 50편 뒤에도 좋은 후보 중앙 60편이 남는데 5페이지엔 3.3/10 만 들어온다.
- 적합/부적합을 가르는 건 **공유 태그 수**(2.05 vs 1.57).
- 웹툰 30%(1,098편)는 정제 후 태그가 **장르명 하나**다. 그런 시드가 **50/67 프로필**에 있고 피복률이 시드 개별 최대 `max_i |A∩S_i|/|S_i|` 라 **그 장르 전 작품이 피복 1.0** 을 받는다.
- 임베딩 텍스트(`text_builder`)는 `x not in set(g)` 로 장르명 태그를 빼는데 **랭커의 태그 정제는 빼지 않는다.**

기계 진단(채점 없음, 67프로필): 스위치를 켜면 top-10 이 **67/67 프로필에서 바뀌고** top-50 겹침 0.642.
장르명 태그를 빼면 시드의 35.2% 가 태그 0개가 된다(그 시드는 피복 기여 0 — 코드 `if s else 0.0`).
"장르 일치 + 비장르 태그 1개 이상 공유" 비율: 1페이지 0.579→0.645 · 5페이지 0.479→0.500 (내 정의라 판정에 쓰지 않는다).

## 가설 H-T10
> 태그 피복에서 작품 자신의 장르명 태그를 빼면 피복률이 장르가 아니라 **실제 취향 라벨**의 겹침을 재게 되어,
> 새로고침 깊은 페이지에서 공유 태그가 약한 작품이 밀려나고 적합률이 오른다.

반증 조건: 5페이지 적합률이 오르지 않는다 — 장르명 포화가 원인이 아니거나, T-3 의 이득이 사실상 장르 보너스였고 그것을 빼면 손해다.
**위험(관측 전 명시)**: top-10 이 전 프로필에서 바뀐다. T-3/T-9 가 잰 `tag_w` 이득 일부가 장르 보너스였다면 1페이지가 떨어질 수 있다 → 1페이지 가드를 건다.

## 변형 — 팔 2개
| 팔 | `tag_drop_genre` | 비고 |
|---|---|---|
| **n0** | False | **현행 서빙** (tag_w 0.2 · 장르명 태그 포함) |
| **n1** | True | 장르명 태그 제외 (tag_w 0.2 그대로) |

- 구현: `personalized_ranker.clean_tags(drop_genre)` · `Engine._tags_ng` · `Engine.recommend(tag_drop_genre=None)` → `PRODUCTION["tag_drop_genre"]` = False(판정 전 서빙 불변). 리팩터 회귀: 기본값 k=50 == T-9 g2 **67/67** 확인.
- 다른 축 전부 PRODUCTION. `tag_w` 는 바꾸지 않는다(한 축).
- 경로: 제품 경로 `Engine.next_page(k=10, seen 누적)` 5회. **빌드 assert**: n0 1페이지 == T-9 g2 top-10 67/67.

## 프로필·표본·채점
**프로필 67 전체** · 팔마다:
- **5페이지(41~50번째 새로고침 칸) 전수 10슬롯** — 주 지표
- **1페이지 10칸 중 5슬롯 무작위** — 가드 (RNG seed 10)
두 팔의 같은 (프로필, 작품) 쌍은 한 번만 채점. 3역할 새로 채점 · 은행 재사용 없음 · 페이지·팔을 섞은 한 시트.
역할 프롬프트 `eval/t1_preregister.md`(md5 2f06edaa5549fac17941f79dcebe85e4) 원문. 시트 meta 는 T-9 와 같다(장르 · 태그 6 · 회차) — 시트 태그는 원본 그대로(정제 규칙과 무관하게 채점자에게 같은 정보).

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 3역할 중 2역할 이상 등급 ≥ 2.

    Δ5 = 다수결(n1, 5페이지) − 다수결(n0, 5페이지)      [팔당 670슬롯]
    Δ1 = 다수결(n1, 1페이지 표본) − 다수결(n0, 1페이지 표본)   [팔당 335슬롯]

- **(A) 채택** — `Δ5 ≥ +0.05` 그리고 `Δ1 ≥ −0.03` → `PRODUCTION["tag_drop_genre"] = True`
- **(B) 보류** — `+0.02 ≤ Δ5 < +0.05` 그리고 `Δ1 ≥ −0.03` → False 유지, 재현 라운드 필요로 기록
- **(C) 기각** — 그 외(Δ5 < +0.02 또는 Δ1 < −0.03) → False 유지

문턱 +0.05 의 근거: §94~§96 — 이 잣대에서 ±0.03~0.05 의 단일 라운드 차이는 재현되지 않았다(창작자 보너스 반전). 잡음 폭을 넘는 효과만 채택한다.

### 부수 관측 (판정 아님)
축별(rule/coh/mix/count) Δ5 · 역할별 · 3페이지는 재지 않는다 · 5페이지 공유 태그 수 평균 · 태그 0개 시드가 있는 프로필 vs 없는 프로필 Δ5 · X-26 웹툰 F5(0.643, 14프로필)와 n0 F5 비교.

## 예상 (관측 전)
**(B) 또는 (A), Δ5 +0.02 ~ +0.06 · Δ1 −0.03 ~ +0.03.** 5페이지 "좋은 후보" 대리 지표가 +0.021 밖에 안 올랐다 — 목록은 크게 바뀌지만(겹침 64%) 깊은 페이지 개선폭은 크지 않을 수 있다.
좁은 취향(coh)에서 가장 크게 오를 것으로 본다.
