# T-9 사전등록 — 웹툰 `tag_w` **재현 라운드** (T-3 채택의 확인) (2026-09-14)

## 왜
T-3(봉인 c2b2a902)은 `tag_w` 0.2 에서 3역할 다수결 합산 **+0.0746** 으로 **(A) 채택**됐고 지금 서빙 중이다.
그런데 창작자 보너스 재현 라운드 두 번(§93 T-8 · §94 V-3)에서 **첫 라운드 +0.028 · +0.056 이 재측정에서 −0.020 · −0.032 로 뒤집혔다.**
같은 쌍 재채점 적합 판정은 12~17% 뒤집힌다. **단일 라운드 채택은 전부 재현을 확인해야 한다.**
T-3 은 크기(+0.075)가 V-2 보다 크고 LOO 67/67 · 부호 +38/−7 로 견고성 지표가 좋았지만, V-2 도 네 잣대가 일치했었다.

## 질문
> T-3 의 +0.075 는 현재 PRODUCTION 위에서 새 슬롯 표본·새 채점으로 다시 재도 나오는가?

## 변형 — 팔 2개
| 팔 | tag_w | 비고 |
|---|---|---|
| **g0** | 0.0 | 태그 항 끔 |
| **g2** | 0.2 | **현행 서빙 목록** |

- 0.4 는 뺀다(T-3 에서 0.2 보다 낮았다).
- 다른 축 전부 현재 `PRODUCTION` (rep_v2 · top2_mean · pop 0 · hub 0 · star 0 · creator 0 · 후처리 동일).
- 빌드 시 **g2 목록이 T-8 r0(= 현행 서빙) 목록과 67/67 일치**함을 assert 한다.
  g0 목록과 T-3 w0 목록의 겹침은 부수로 보고한다(T-3 이후 코드·설정이 바뀌었는지 확인용).

## 프로필·표본·채점
프로필 67 · k=50 · 슬롯 1-10 중 5 + 11-50 중 5 · **RNG seed 9** · **미채점 0 전 비교 금지** · 이전 라운드 채점 재사용 없음.
3역할 모두 새로 채점. 역할 프롬프트는 `eval/t1_preregister.md`(md5 2f06edaa5549fac17941f79dcebe85e4) 원문 그대로. 시트 meta 동일(장르·태그 6·회차).

**주 지표는 T-3 이 채택할 때 쓴 잣대 그대로 — 3역할 다수결 합산 · 프로필 67 · k=50.**
(창작자 축에서 역할 B 단독으로 옮긴 이유는 인기 편향이었다. 태그 피복은 인기 축이 아니라 원래 잣대로 재현한다.) 역할 B 단독은 부수 관측.

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 3역할 중 2역할 이상 등급 ≥ 2.

    Δ = 다수결 합산(g2) − 다수결 합산(g0)

**이 축은 서빙 중이다. 판정은 유지하느냐 되돌리느냐다.**
- **(A) 재현 성공** — `Δ ≥ +0.03` · (E) ILS@50 g2 < g0 + 0.03 · (F) mix 12 다수결 g2 ≥ g0 − 0.05 → `tag_w = 0.2` **유지·확정**
- **(B') 약한 재현** — `+0.02 ≤ Δ < +0.03` · (E)(F) 통과 → `tag_w = 0.2` **유지**, 크기 불확실로 기록
- **(C) 재현 실패** — `Δ < +0.02` 또는 (E)/(F) 위반 → **`tag_w = 0` 으로 되돌린다**

T-3 과 합쳐 문턱을 다시 재는 규칙은 두지 않는다. 합산은 목록이 같을 때만 부수로 적는다.

### 부수 관측 (판정 아님)
역할 B 단독 Δ · 역할 A/C · 1-10/11-50 · 축별(rule/coh/mix/count) · 태그 피복 top-50 · T-3 과 같은 쌍의 다수결 일치율.

## 예상 (관측 전)
**(A), Δ +0.03 ~ +0.07.** 창작자 축과 달리 T-3 은 (1) 크기가 잡음 폭(±0.03)의 2배 이상, (2) LOO 67/67 이 모두 +0.03 이상,
(3) 목록 변화의 기제(시드와 **다른 장르**로 새던 28% 를 같은 장르로 교체, 시드 장르 일치 0.724→0.855)가 채점과 무관하게 관측됐다.
다만 V-2 도 기제가 보였는데 재현에 실패했으므로 확신하지 않는다. 첫 라운드보다 작게 나올 것으로 본다.
