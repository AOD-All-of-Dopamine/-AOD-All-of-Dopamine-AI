# T-1 판정 — 웹툰 표현: **(A) rep_v2(태그 포함) 채택** (2026-09-04)

사전등록 `eval/t1_preregister.md` md5 **2f06edaa5549fac17941f79dcebe85e4** (봉인 유지).
신규 1,229쌍 · 만장일치 0.85 · **미채점 0** · 보정 항 전부 0 · 웹툰 채점 프롬프트 원문(사전등록 §채점).

## 결과 — 프로필 67(전부 기계 생성) · 팔당 670슬롯

| 팔 | 1-10 | 11-50 | **합산** |
|---|---|---|---|
| rep_v1 (제목+장르+줄거리) | 0.761 | 0.701 | **0.7313** |
| **rep_v2 (+태그)** | **0.836** | **0.770** | **0.8030** |

**Δ = +0.0716** → 문턱 "≥ +0.03" → **(A) rep_v2 채택.**

**예상은 틀렸다.** (B) 동률(|Δ|<0.03)을 가장 그럴듯하다고 적었다. 게이트 지표(변별력 +0.004 차,
제목 단어 겹침 동률)가 둔했을 뿐, 채점으로는 **분명하게** 갈렸다. 프로필 분해 **35 : 10 : 22**(rep_v2 승·패·비슷).

## 어디서 이기나 — 전부 같은 방향이다 (판정 아님)

| 축 | rep_v1 | rep_v2 | Δ |
|---|---|---|---|
| rule (장르×인기) | 0.762 | 0.831 | +0.069 |
| coh (한 장르 5시드) | 0.820 | **0.930** | **+0.110** |
| mix (두 장르) | 0.608 | 0.633 | +0.025 |
| count (시드 1/3/5/10) | 0.713 | 0.800 | +0.087 |

| 인기 대역 | rep_v1 | rep_v2 | Δ |
|---|---|---|---|
| high | 0.822 | 0.900 | +0.078 |
| mid | 0.760 | 0.800 | +0.040 |
| **low** | 0.710 | 0.800 | **+0.090** |

사전등록의 부수 가설 — "태그가 이긴다면 줄거리가 얇은 저관심 작품에서 이길 것" — 이 맞았다:
**low 대역에서 +0.090** 으로 가장 크다(mid +0.040). 웹툰 줄거리 중앙이 126자라 태그가 부족한 의미를 채운다.
`mix`(두 장르 혼합)는 +0.025 로 가장 작다 — 두 표현 모두 여기서 낮고(0.61/0.63), X-25 가 보인
"지표가 다양성을 벌준다"가 웹툰에서도 그대로다.

ILS@50: rep_v1 0.515 · rep_v2 0.523 — 태그가 목록을 더 좁히지는 않는다(+0.008).

## 한계
- 보정 0 에서 잰 숫자다. **제품 숫자가 아니다.** 기준선은 rep_v2 위에서 다시 잰다.
- LLM 채점이다. 잣대의 성질(X-22·X-25)은 여기도 그대로다 — `mix` 가 낮은 것이 그 흔적이다.
- 태그 정지어 목록(`완결*` 등 12종)은 이 판정의 일부다. 목록을 바꾸면 재판정 대상이다.

## 처분
1. **`artifacts/wt_v1`(rep_v2)을 웹툰 확정 표현으로 기록한다.** `config.py` 에 T-1 로 적는다.
2. `artifacts/rep_v1` 은 대조군으로 보존한다.
3. 다음 축: rep_v2 위에서 기준선(보정 전부 0) → `pop_boost` → `tag_w` → `strategy` → `hub_lambda` → `star_boost`.
