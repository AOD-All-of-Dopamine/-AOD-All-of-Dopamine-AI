# T-8 사전등록 — 웹툰 `creator_w` **재현 라운드** (T-7 보류의 후속) (2026-09-14)

## 왜
T-7(봉인 8454695c)은 `creator_w` 0.10 에서 역할 B **+0.028** 로 **(B) 보류**였다. 방향은 세 역할 모두 +였고
단조·저관심·ILS·mix 조건은 전부 통과했지만 채택 문턱 +0.04 에 못 미쳤다.
§84 가 잰 잣대의 슬롯 표본 잡음 폭이 **0.037** 이라, +0.028 은 **잡음 한 번의 크기 안에 있다.**
같은 목록을 **새 슬롯 표본·새 채점**으로 한 번 더 재서 이 효과가 재현되는지 본다.

## 이 라운드가 답하는 질문
> T-7 의 +0.028 은 실제 효과인가, 한 번의 표본 잡음인가?

## 변형 — 팔 2개 (T-7 의 c0 · c10 과 **같은 목록**)
| 팔 | creator_w |
|---|---|
| **r0** | 0.00 |
| **r10** | 0.10 |

- 0.25 는 뺀다: T-7 에서 0.10 보다 낮았고(역U) 채택 후보가 아니다.
- 다른 축 전부 `PRODUCTION` 고정 (rep_v2 · top2_mean · pop 0 · hub 0 · star 0 · tag_w 0.2 · 후처리 series 1 · artist 2 · adult 제외).
- 빌드 시 r0 · r10 목록이 T-7 `c0` · `c10` 과 **67/67 완전 일치**하는지 assert 한다 (다르면 재현 라운드가 아니다).

## 프로필·표본·채점
프로필 67 · **주 판정 대상군 64** (T-7 과 동일 정의) · k=50 · 슬롯 1-10 중 5 + 11-50 중 5 ·
**RNG seed 8** (T-7 은 7) · **미채점 0 전 비교 금지.**
**T-7 채점을 재사용하지 않는다** — 같은 (프로필, 작품) 쌍이 다시 뽑혀도 새로 채점한다. 재현하려는 것은 표본 잡음과 채점 잡음 둘 다다.
3역할 모두 수집한다. 판정은 **역할 B 단독**, 역할 A 는 저관심 안전, 역할 C 는 부수 관측 — T-7 과 동일.
시트 meta 는 T-7 과 동일(장르·태그 6·회차 수). 작가·관심수·평점은 넣지 않는다.
역할 프롬프트는 `eval/t1_preregister.md`(md5 2f06edaa5549fac17941f79dcebe85e4) 원문 그대로다 (T-7 사전등록에 전문 수록).

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 그 역할 등급 ≥ 2. 주 지표 = **역할 B 적합률 · 대상군 64 · k=50 합산.** 저관심 = T-7 과 같은 17개(시드 관심수 중앙 ≤ 28,706).

    Δ_B = B(r10) − B(r0)

- **(A) 채택** — 전부 만족: `Δ_B ≥ +0.04` · 저관심 17 역할 A `Δ_A ≥ −0.03` · (E) ILS@50 r10 < r0 + 0.03 · (F) mix 12 역할 B r10 ≥ r0 − 0.05
  → `creator_w = 0.10` 채택
- **(B') 두 번째 보류 → 축 종료** — `+0.02 ≤ Δ_B < +0.04` (안전장치 통과)
  → `creator_w = 0` 유지. **T-7·T-8 연속 보류는 "효과가 있더라도 이 잣대로 채택할 만큼 크지 않다"로 기록하고 이 축을 닫는다.** 세 번째 라운드는 하지 않는다.
- **(C) 재현 실패** — `Δ_B < +0.02`, 또는 안전장치 위반
  → `creator_w = 0` 유지, T-7 의 +0.028 은 재현되지 않은 것으로 기록하고 축을 닫는다.

**문턱을 낮추지 않는다.** 특히 T-7 과 T-8 을 **합쳐서 문턱을 다시 재는 규칙을 두지 않는다** — T-7 결과(+0.028)를 본 뒤에
합산 문턱을 정하면 그 숫자에 맞춰 문턱을 고른 것이 된다. 합산 Δ 는 **부수 관측으로만** 적는다.

### 부수 관측 (판정 아님)
T-7·T-8 합산 역할 B Δ(프로필당 20슬롯) · 3역할 다수결 · 역할 A/C · 67 전체 · 축별(rule/coh/mix/count) ·
T-7 과 T-8 에서 **같은 (프로필, 작품) 쌍이 뽑힌 경우** 역할 B 등급 일치율(채점 잡음 추정).

## 예상 (관측 전)
**(B') 또는 (C), Δ_B +0.005 ~ +0.03.** 채택 문턱 근처에서 멈춘 첫 결과는 재측정에서 작아지는 경향이 있다(승자의 저주).
T-7 에서 세 역할이 모두 +였고 mix(+0.092)·1-10(+0.044)이 뚜렷해서 **방향은 유지**될 것으로 보지만,
+0.04 를 넘길 가능성은 낮다고 본다.
