# V-4 사전등록 — TMDB `genre_w` **재현·재판정 라운드** (D-44 채택의 확인) (2026-09-14)

## 왜
`genre_w` 0.40 은 D-44(2026-08-22)에서 채택돼 서빙 중이다. 두 가지 이유로 확인이 필요하다.
1. **단일 라운드 채택이다.** 창작자 보너스 재현 두 번(§93 · §94)이 첫 라운드 +를 모두 뒤집었다.
2. **다른 조건에서 쟀다.** D-44 는 (a) 한국어 줄거리 필터가 켜진 33,728편 풀, (b) 1인 채점 은행, (c) dev/val 분할에서 쟀다.
   V-1(§89)에서 풀이 59,780편으로 바뀌었고 채점도 3역할 다수결로 바뀌었다. **V-1 이 "나머지 네 축 재판정"을 남겨 두었다.**

그래서 이 라운드는 엄밀한 재현이 아니라 **현재 조건에서의 재판정**이다.

## 질문
> 현재 PRODUCTION(59,780편 풀 · 3역할 다수결)에서 `genre_w` 0.40 은 0 보다 나은가?

## 변형 — 팔 2개
| 팔 | genre_w | 비고 |
|---|---|---|
| **h0** | 0.00 | 장르 피복 항 끔 |
| **h4** | 0.40 | **현행 서빙 목록** |

- 다른 축 전부 현재 `PRODUCTION` (strategy mean · hub 0.35 · rating 0.15 · media 0.20 · align 0 · director 0 · require_korean False · 후처리 동일).
- 빌드 시 **h4 목록이 V-3 e0(= 현행 서빙) 목록과 52/52 일치**함을 assert 한다.

## 프로필·표본·채점
프로필 52 · k=50 · 슬롯 1-10 중 5 + 11-50 중 5 · **RNG seed 15** · **미채점 0 전 비교 금지** · 이전 라운드 채점 재사용 없음.
3역할 모두 새로 채점. 역할 프롬프트는 `eval/v1_preregister.md`(md5 309e7d0d55b09fe6d5ec4fbaad6967cb) 원문 그대로. 시트 meta 는 V-1~V-3 과 동일.

**주 지표는 V-1 이 새 기준선을 정할 때 쓴 잣대 — 3역할 다수결 합산 · 프로필 52 · k=50.**
(장르 피복은 인기 축이 아니다.) 역할 B 단독은 부수 관측.

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 3역할 중 2역할 이상 등급 ≥ 2. 롱테일 = 시드 투표수 중앙 하위 25% 13개(V-2·V-3 과 동일).

    Δ = 다수결 합산(h4) − 다수결 합산(h0)

**이 축은 서빙 중이다. 판정은 유지하느냐 되돌리느냐다.**
- **(A) 재현 성공** — `Δ ≥ +0.03` · 롱테일 13 다수결 `Δ ≥ −0.03` · **붕괴 안전장치**: 한 프로필이라도 `−0.30` 이하면 채택 근거가 흔들린다 —
  이 경우 (A) 대신 (B') 로 내린다 → `genre_w = 0.40` **유지·확정**
- **(B') 약한 재현** — `+0.02 ≤ Δ < +0.03` 이고 롱테일 안전 → `genre_w = 0.40` **유지**, 크기 불확실로 기록
- **(C) 재현 실패** — `Δ < +0.02` 또는 롱테일 `Δ < −0.03` → **`genre_w = 0` 으로 되돌린다**

붕괴 안전장치는 D-43 이 자카드를 기각한 이유(five_horror −0.120)를 이어받은 것이다. 프로필당 10슬롯이라 −0.30 은 3슬롯 차이다.

### 부수 관측 (판정 아님)
역할 B 단독 Δ · 역할 A/C · 1-10/11-50 · 프로필별 Δ 분포 · five_horror · 매체/장르 일치율 · V-3 과 같은 쌍의 다수결·역할 B 일치율.

## 예상 (관측 전)
**(A) 또는 (B'), Δ +0.02 ~ +0.05.** D-44 는 1인 채점에서도 val 확증 +0.035·+0.040 으로 두 분할에서 같은 방향이었다(사실상 한 번의 내부 재현).
장르 피복은 V-2 감독 항보다 목록을 훨씬 크게 바꾼다(발화 대상이 전 프로필) — 효과가 실재한다면 잡음 위로 드러나기 쉽다.
위험: 풀이 26,051편 늘며 장르만 맞는 저투표 비한국어 작품이 올라올 수 있다 — 11-50 에서 손해가 날 수 있다.
