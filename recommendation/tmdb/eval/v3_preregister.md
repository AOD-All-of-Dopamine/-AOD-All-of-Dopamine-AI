# V-3 사전등록 — TMDB `director_w` **재현 라운드** (V-2 채택의 확인) (2026-09-14)

## 왜
V-2(봉인 cd9687a9)는 `director_w` 0.10 에서 역할 B **+0.056** 으로 **(A) 채택**됐고 지금 `PRODUCTION` 에 들어가 있다.
그런데 웹툰 T-8(§93)에서 두 가지를 알게 됐다.
1. 첫 라운드 +0.028(세 역할 모두 +)이 같은 목록 재측정에서 **−0.020** 으로 뒤집혔다.
2. 같은 쌍 658건 재채점에서 역할 B 적합 여부 일치가 **0.827** — 적합 판정의 17% 가 뒤집힌다.

V-2 는 **한 번만 쟀다.** 크기가 T-7 의 두 배라 잡음일 가능성은 더 낮지만, 확인하지 않은 채 서빙하고 있다.

## 이 라운드가 답하는 질문
> V-2 의 +0.056 은 같은 목록을 새 슬롯 표본·새 채점으로 다시 재도 나오는가?

## 변형 — 팔 2개 (V-2 의 d0 · d10 과 **같은 목록**)
| 팔 | director_w |
|---|---|
| **e0** | 0.00 |
| **e10** | 0.10 |

- 0.25 는 뺀다: V-2 에서 0.10 보다 낮았고(역U) 서빙 값이 아니다.
- 다른 축 전부 `PRODUCTION` 고정 (strategy mean · hub 0.35 · rating 0.15 · media 0.20 · genre 0.40 · align 0 · require_korean False · 후처리 동일).
  **현재 PRODUCTION 의 director_w 는 0.10 이므로 e0 은 명시적으로 0 을 넣는다.**
- 빌드 시 e0 · e10 목록이 V-2 `d0` · `d10` 과 **52/52 완전 일치**하는지 assert 한다.

## 프로필·표본·채점
프로필 52 · **주 판정 대상군 50** (V-2 와 동일, `v2_key.json` 의 target) · k=50 · 슬롯 1-10 중 5 + 11-50 중 5 ·
**RNG seed 14** (V-2 는 12) · **미채점 0 전 비교 금지.**
**V-2 채점을 재사용하지 않는다** — 같은 쌍이 다시 뽑혀도 새로 채점한다.
3역할 모두 수집. 판정은 **역할 B 단독**, 역할 A 는 롱테일 안전, 역할 C 는 부수 관측 — V-2 와 동일.
시트 meta 는 V-2 와 동일(매체 · 장르 · 키워드 6 · 줄거리 80자). 감독·인기·평점은 넣지 않는다.
역할 프롬프트는 `eval/v2_preregister.md`(md5 cd9687a9d807c6ef395f52fec94a090a)에 수록된 원문(V-1 원문) 그대로다.

## 판정 (관측 전 고정 — 이후 움직이지 않는다)
적합 = 그 역할 등급 ≥ 2. 주 지표 = **역할 B 적합률 · 대상군 50 · k=50 합산.**
롱테일 = 시드 투표수 중앙이 52프로필 중 하위 25% (= 13개, V-2 와 동일). 

    Δ_B = B(e10) − B(e0)

**이 축은 이미 채택돼 서빙 중이다.** 그래서 판정은 "유지하느냐 되돌리느냐"다.

- **(A) 재현 성공** — `Δ_B ≥ +0.04` 이고 롱테일 역할 A `Δ_A ≥ −0.03` → `director_w = 0.10` **유지 · 확정**
- **(B') 약한 재현** — `+0.02 ≤ Δ_B < +0.04` 이고 롱테일 안전 → `director_w = 0.10` **유지**, "효과 크기 불확실"로 기록
  - 근거(관측 전): T-8 이 보인 단일 라운드 잡음은 ±0.025 규모다. 독립된 두 라운드가 **둘 다 +0.02 이상**이면
    잡음만으로 설명하기 어렵고, 롱테일 손해가 없는 보너스형 항이라 유지의 비용이 작다.
- **(C) 재현 실패** — `Δ_B < +0.02`, 또는 롱테일 `Δ_A < −0.03` → **`director_w = 0` 으로 되돌린다.**
  `config.py` PRODUCTION 을 수정하고, V-2 채택은 재현되지 않은 것으로 기록한다.

**문턱을 낮추지 않는다. V-2 와 V-3 을 합쳐 문턱을 다시 재는 규칙을 두지 않는다** (합산은 부수 관측으로만).

### 부수 관측 (판정 아님)
V-2+V-3 합산 역할 B Δ(프로필당 20슬롯) · 3역할 다수결 · 역할 A/C · 52 전체 · 1-10/11-50 ·
V-2·V-3 에서 같은 쌍 재채점의 역할 B 등급·적합 일치율 (웹툰 T-8 의 0.614/0.827 과 비교).

## 예상 (관측 전)
**(A) 또는 (B'), Δ_B +0.02 ~ +0.05.** V-2 는 1-10 +0.080 · 3역할 다수결 +0.072 · 역U 모양으로 신호가 T-7 보다 훨씬 강했고,
`coh_nolan` 에 테넷이 들어오는 것처럼 **기계적으로 보이는 교정**(감독 일치@10 0.31 → 1.23)이 있었다.
다만 첫 라운드 크기는 재측정에서 줄어드는 경향이 있어(승자의 저주) +0.056 보다는 작게 나올 것으로 본다.
