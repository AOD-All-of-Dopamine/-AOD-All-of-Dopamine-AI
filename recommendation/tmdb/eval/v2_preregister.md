# V-2 사전등록 — TMDB **감독 정합** `director_w` (2026-09-13)

## 왜
임베딩에 감독이 없다. `semantic_text` 는 줄거리·키워드·장르뿐이고, 제목조차 일부러 뺐다
(`text_builder.py`: 프랜차이즈 이름이 속편을 뭉치게 한다). 그래서 **"이 감독을 좋아한다"는
취향을 시스템이 표현할 방법이 지금은 없다.**

실측 표현형: `coh_nolan`(시드 인터스텔라·인셉션·다크 나이트) 의 top-10 에 놀란 영화가 0편이고
대신 마블이 들어온다. 다크 나이트가 시스템에게는 「액션·범죄·스릴러 슈퍼히어로물」이라
어벤져스가 가까운 이웃이기 때문이다. 시드가 준 정보만 보면 타당한 이웃이지만,
사람이 "놀란"이라고 이름 붙인 의도는 시드 세 편에 담기지 않는다.

**감독을 임베딩에 넣지 않고 랭킹 항으로 둔다.** 이유 셋:
재임베딩 10시간(실측 1.71건/s × 59,780)인데 효과를 따로 못 잰다 · 이름을 토큰으로 섞으면
가중치를 못 정하고 못 끈다 · 정확 집합 일치가 토큰 근사보다 노이즈가 없다.

## 데이터 (2026-09-13 수집)
`artifacts/tmdb_v1/directors.parquet` — 코퍼스 59,780 중 **59,776건 피복(100.0%)**.
사람 **id**(정수)로 맞춘다. 이름은 언어 설정에 따라 갈리고 동명이인이 있어 매칭에 쓰지 않는다.

| | 감독 정보 있음 |
|---|---|
| 영화 50,301 | 50,228 (99.9%) — crew 의 `job == "Director"` |
| 드라마 9,479 | 6,647 (70.1%) — `created_by` (크리에이터) |

드라마 crew 에는 Director 가 **없다**(실측: 브레이킹 배드 crew job 목록에 부재). 회차마다 달라
시즌 단위로는 의미가 없기 때문이다. 정보가 없는 후보는 정합 0 — **감점이 아니라 무보정**이다.

## 수식
`genre_w`(D-44) 와 **같은 형태**다. 시드 **개별** 최대 피복률, 분모에 후보를 넣지 않는다.

    dir_align(A) = max over seeds S_i  of  |dirs(A) ∩ dirs(S_i)| / |dirs(S_i)|
    base *= (1 + director_w × dir_align)

감독은 보통 1명이라 사실상 "이 시드와 같은 감독인가"의 이진 지시자가 된다.

## 격자 — 팔 3개
| 팔 | `director_w` | w=0 과 겹침@50 |
|---|---|---|
| **d0** | 0.00 | 100% (기준선) |
| **d10** | 0.10 | 96% |
| **d25** | 0.25 | 89% |

다른 축은 전부 `PRODUCTION` 고정: `strategy=mean` · `hub_lambda=0.35` · `rating_boost=0.15` ·
`media_w=0.20` · `genre_w=0.40` · `align_w=0.0` · `require_korean=False`.
후처리 `PRODUCTION_POSTPROCESS` 동일.

**0.05 와 0.50·1.00 을 격자에서 뺀 이유 (채점 전, 기계적 근거).**
- `0.05` 는 w=0 과 top-50 이 **99% 겹친다.** §84 가 잰 이 잣대의 자체 잡음 폭이 0.037 인데,
  목록의 1% 차이는 그 아래다. 잴 수 없는 것을 팔에 넣으면 채점 예산만 쓴다.
- `0.50`·`1.00` 은 52프로필 평균 감독일치@10 이 5.33·7.33 이 된다(w=0 은 0.31).
  `twenty_library` 는 **top-10 전부**가 감독 일치가 된다. 목록의 절반 이상이 한 축에 지배되면
  그건 랭킹이 아니라 필터다. 채점 없이 기계적으로 배제한다.

## 대상군
**감독 축이 발화할 수 있는 프로필 = 시드 감독의 다른 작품이 코퍼스에 1편 이상.**
등급을 보지 않고 정의된다. **50/52** 가 대상이다.

비대상 2개: `five_tv_lowvote`(크리에이터 4명이 전부 그 작품 1편뿐) · `lowvote_animation`
(시드 3편 모두 감독 정보 없음). 이 둘은 팔이 달라도 목록이 **완전히 동일**하므로 Δ 에
정확히 0 을 기여하고, 포함하면 Δ 를 50/52 배로 희석할 뿐이다.

- **주 판정은 대상군 50.**
- 52 전체는 부수 관측으로 같이 적는다.

## 프로필·표본·채점
프로필 52(전체 생성, 판정은 50) · k=50 · 슬롯 **1-10 중 5 + 11-50 중 5** · **RNG seed 12** ·
**미채점 0 전 비교 금지**. 라운드 안에서만 은행 재사용(같은 (프로필, 후보) 쌍은 한 번만 채점).

**3역할을 모두 수집한다. 다만 판정은 역할 B 단독이 한다.** 역할 프롬프트는 V-1
(md5 309e7d0d55b09fe6d5ec4fbaad6967cb)의 원문을 그대로 쓴다 — 아래 봉인.

### 왜 B 단독인가 (§84 진단)
885쌍에서 역할별 인기 편향을 쟀다:

| 역할 | corr(등급, log 투표수) | 유명 vs 무명 적합률 차 |
|---|---|---|
| A 평가자 ("인기·평점은 보지 마세요") | 0.063 | +0.014 |
| B 헤비유저 | 0.227 | +0.135 |
| C 편집자 | 0.188 | +0.086 |

감독 정합은 **유명 감독의 유명작**을 끌어올리는 장치다. A 는 지시상 인기를 절반쯤 중화하므로,
3역할 다수결로 재면 이 축에 구조적으로 불리하다. 지난 네 번의 인기 축 기각이 이 함정 위에 있었다.
그래서 **B("이 추천을 보면 실제로 눌러 볼까")가 채택을 정하고, A 가 안전을 확인한다.**

**대가를 명시한다: B 단독은 다수결이 아니라 잡음이 안 깎인다.** §84 의 잡음 폭 0.037 은
3역할 다수결에서 잰 값이고 **B 단독의 잡음은 재본 적이 없다 — 더 클 것이다.** 그래서 채택 문턱을
기존 +0.03 이 아니라 **+0.04** 로 올리고, 단조성 조건을 하나 더 건다(아래).
**이 라운드 숫자는 과거 라운드와 비교하지 않는다** (§84 의 라운드 간 비교 금지가 그대로 적용).

### 역할 A
> 당신은 추천 시스템의 품질 평가자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 영화·드라마 시드 목록(seed)과 시스템이 추천한 후보(cand, meta)입니다.
> 각 후보가 그 시드를 좋아하는 사용자에게 얼마나 관련 있는 추천인지 등급을 매기세요: 3 = 강한 적합 (시드와 장르·소재·톤이 명확히 맞음), 2 = 적합 (같은 계열, 자연스러운 추천), 1 = 약함 (겹치는 요소가 하나뿐이거나 결이 다름), 0 = 무관. 기준은 오직 시드와의 관련성입니다. 인기·평점은 보지 마세요. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

### 역할 B
> 당신은 영화·드라마를 매우 많이 보는 헤비 시청자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 어떤 사용자가 좋아한 시드 목록(seed)과 앱이 그 사용자에게 추천한 후보(cand, meta)입니다.
> 당신이 그 시드들을 좋아하는 사람이라고 상상하고 "이 추천을 보면 실제로 눌러 볼까?"로 등급을 매기세요: 3 = 바로 볼 것 같다, 2 = 볼 만하다 (납득되는 추천), 1 = 굳이? (겹치는 게 하나뿐이거나 결이 다름), 0 = 왜 이게 나왔지. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

### 역할 C
> 당신은 영상 스트리밍 서비스의 편집자/큐레이터입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 시드 목록(seed)과 알고리즘이 추천한 후보(cand, meta)입니다.
> "이 시드를 좋아하는 사용자에게 보여주는 추천 목록에 이 후보를 넣어도 편집자로서 부끄럽지 않은가"로 등급을 매기세요: 3 = 자신 있게 넣는다, 2 = 넣어도 된다, 1 = 넣기 애매하다 (겹치는 요소가 약하거나 톤·연령대 등 결이 다름), 0 = 빼야 한다. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

## 판정 (관측 전 고정 — 이후 절대 움직이지 않는다)
적합 = 그 역할의 등급 ≥ 2. 주 지표 = **역할 B 적합률, 대상군 50 프로필, k=50 합산.**

    Δ_B(w) = B적합률(w) − B적합률(0)
    Δ_A,longtail(w) = A적합률(w, 롱테일) − A적합률(0, 롱테일)

롱테일 = 시드 투표수 중앙이 52프로필 중 하위 25%(= 13개). 등급을 보지 않고 정의된다.

- **(A) 채택** — 아래 **셋 다** 만족하는 w 가 있을 때, 그중 Δ_B 가 가장 큰 w 를 확정값으로 한다
  1. `Δ_B(w) ≥ +0.04`
  2. **단조성**: `B적합률(0) < B적합률(0.10) ≤ B적합률(0.25)` 또는
     `B적합률(0) < B적합률(0.25) ≤ B적합률(0.10)` — 즉 두 켠 팔 모두 기준선보다 높다.
     잡음이 우연히 한 팔만 밀어 올린 경우를 배제한다.
  3. **안전**: `Δ_A,longtail(w) ≥ −0.03`
- **(B) 보류** — `+0.02 ≤ Δ_B(최고팔) < +0.04` 이거나 단조성만 깨진 경우.
  `director_w = 0` 유지하고 **재현 라운드가 필요하다고 기록**한다. 문턱을 낮추지 않는다.
- **(C) 기각** — 그 외. `director_w = 0` 유지. 문턱을 낮춰 다시 재지 않는다.
- **안전 조건 3 만 깨지면** Δ_B 가 아무리 커도 **채택하지 않는다**. 롱테일 붕괴는
  D-60·T-2·T-6·W-1 에서 네 번 반복된 실패 모드다.

### 부수 관측 (판정 아님)
3역할 다수결 적합률(§84 편향이 실제로 판정을 뒤집는지 한 번 확인) · 52 전체 Δ_B ·
ILS@50 · 감독 일치 편수@10 · 역할 A 단독 Δ · 팔별 top-50 고유 항목 수.

## 예상 실패 모드 (관측 전 기록)
**(B) 보류, Δ_B +0.01 ~ +0.03 에서 갈린다고 본다.**

근거: 감독 축은 프로필마다 발화량이 극단적으로 다르다. `twenty_library` 는 감독 일치 후보가
183편인데 `longtail_family` 는 1편이다. 50개 대상군의 평균은 소수 프로필의 큰 변화가
다수의 0 변화에 희석된다. w=0.10 에서 top-50 이 96% 겹친다는 건 **슬롯 10개 중 평균 0.4개만
바뀐다**는 뜻이고, 그 정도로 +0.04 를 넘기려면 바뀐 슬롯이 거의 전부 개선이어야 한다.

**구체적 실패 시나리오 하나를 미리 적어 둔다.** 다작·편차형 감독이 태작을 끌어올린다.
`five_horror`(시드 투표 중앙 8,754) 의 w=0.25 top-10 에서 관측:
「악령의 관」 평점 6.4 투표 565 · 「마이 소울 투 테이크」 5.5/559 · 「우리집의 이방인」 5.3/115
— 전부 웨스 크레이븐(24편, 필모의 42%가 평점 6.0 미만) 과 존 카펜터(24편)다.
**이게 나쁜 추천인지는 이 사전등록이 정하지 않는다.** 공포 팬에게 반가울 수도 있다.
눈가림 채점자가 답할 질문이고, 그래서 안전장치를 미리 넣지 않았다.

**이번 라운드에 넣지 않은 것 (기각되든 채택되든 별건으로 다룬다):**
- **시드 상대 투표수 하한** — 위 실패 시나리오가 가리키는 장치. 절대 하한은 롱테일을 깬다
  (`longtail_thriller` 시드 투표 중앙 33). "시드 투표수 중앙의 N배 미만이면 부스트 보류" 형태.
- **감독 일치 상한** — top-10 중 같은 감독 최대 n 편 (후처리 `cap_*` 와 같은 모양).
- **n편 이상 게이트** — 필모가 작을수록 태작이 많다는 코퍼스 실측
  (2편 감독: 태작 38.4%·투표 500 미만 90.3% / 40편+: 19.7%·86.7%, corr(log 편수, 태작비율) −0.099)
  이 지지하지만, 위 실패 케이스의 감독이 전부 16~24편이라 **관측된 실패를 하나도 못 막는다.**
- **배우·각본 정합** — 커버리지가 훨씬 넓지만 노이즈도 크다. 감독 결과를 보고 정한다.
