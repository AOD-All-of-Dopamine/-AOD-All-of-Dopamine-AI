experiment:
  id: "steam_p1_v1"
  seed: 42

aggregation:
  strategies: ["max", "mean", "top2_mean"]
  top_n: 300
  rec_boost: 0.03
  # D-37 확정. quality = clip(log10(1+리뷰수)/5, 0, 1) 을 곱 보정으로 더한다.
  # 52프로필 × k=50 · 미채점 0 에서 0.4638 → 0.6608.
  quality_w: 0.10

profiles:
  n_coherent: 10
  n_multi_interest: 10
  split: [12, 8]

paths:
  profiles: "artifacts/p1/profiles.parquet"
  ranked_dir: "artifacts/p1/"
  export: "artifacts/p1/p1_export.xlsx"
