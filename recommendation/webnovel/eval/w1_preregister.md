# W-1 사전등록 — 웹소설 `pop_boost` **재판정** (코퍼스 wn_v6) (2026-09-04)

## 왜 다시 재나
`pop_boost=0.03` 은 D-66 에서 확정했지만 그 판정은 **파일럿 코퍼스(wn_v4 · 7,062편)** 위에서 났다.
전체 코퍼스 **wn_v6(29,494편)** 에서 같은 프로필·같은 설정의 top-50 이 v4 와 **제목 기준 중앙 25% 만 겹친다**.
목록의 3/4 이 다른 작품이므로 확정값을 그대로 물려받을 근거가 없다(§85).

## 이번 라운드에서 바뀌는 채점 방식 (개선, 관측 전 고정)
D-66 은 **나 혼자** 눈가림 채점했다. W-1 은 웹툰 T-시리즈와 같은 **독립 3역할 다수결**로 올린다.
적합 = 3역할 중 **2역할 이상이 등급 2 이상**. 프롬프트 원문을 아래에 봉인한다.

### 역할 A
> 당신은 추천 시스템의 품질 평가자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 웹소설 시드 목록(seed)과 시스템이 추천한 웹소설 후보(cand, meta)입니다.
> 각 후보가 그 시드를 좋아하는 사용자에게 얼마나 관련 있는 추천인지 등급을 매기세요: 3 = 강한 적합 (시드와 장르·소재·톤이 명확히 맞음), 2 = 적합 (같은 계열, 자연스러운 추천), 1 = 약함 (겹치는 요소가 하나뿐이거나 결이 다름), 0 = 무관. 기준은 오직 시드와의 관련성입니다. 인기·평점은 보지 마세요. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

### 역할 B
> 당신은 웹소설을 매우 많이 읽는 헤비 독자입니다. 파일 {FILE} 을 읽으세요. 각 항목은 어떤 사용자가 좋아한 웹소설 시드 목록(seed)과 앱이 그 사용자에게 추천한 웹소설 후보(cand, meta)입니다.
> 당신이 그 시드들을 좋아하는 사람이라고 상상하고 "이 추천을 보면 실제로 눌러 볼까?"로 등급을 매기세요: 3 = 바로 볼 것 같다, 2 = 볼 만하다 (납득되는 추천), 1 = 굳이? (겹치는 게 하나뿐이거나 결이 다름), 0 = 왜 이게 나왔지. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

### 역할 C
> 당신은 웹소설 플랫폼의 편집자/큐레이터입니다. 파일 {FILE} 을 읽으세요. 각 항목은 사용자가 좋아한 웹소설 시드 목록(seed)과 알고리즘이 추천한 웹소설 후보(cand, meta)입니다.
> "이 시드를 좋아하는 사용자에게 보여주는 추천 목록에 이 후보를 넣어도 편집자로서 부끄럽지 않은가"로 등급을 매기세요: 3 = 자신 있게 넣는다, 2 = 넣어도 된다, 1 = 넣기 애매하다 (겹치는 요소가 약하거나 톤·연령대 등 결이 다름), 0 = 빼야 한다. 모르는 작품은 제목·meta 로 추정하세요.
> 결과 JSON 한 줄 {"id": 등급, ...}(파일의 모든 항목)을 파일 {OUT} 에 저장하고, 최종 답변으로는 "저장 완료 N건"만 쓰세요.

**은행 재사용 금지.** 기존 4,372쌍은 1인 채점이라 3역할 다수결과 섞으면 판정이 오염된다. W-1 은 전부 새로 채점한다.

## 변형 — wn_v6 · 교정 프로필(`profiles_v6.parquet`) · top2_mean · rating 0 · hub 0 · mmr 1.0 고정
| 팔 | pop_boost | 비고 |
|---|---|---|
| **p0** | 0.00 | 기준선 |
| **p03** | 0.03 | D-66 확정값 (파일럿 코퍼스에서) |
| **p10** | 0.10 | |

## 프로필·표본
프로필 52 · k=50 · 슬롯 **1-10 중 5 + 11-50 중 5** · **RNG seed 7** · 미채점 0 전 비교 금지.

## 판정 (관측 전 고정) — 합산 기준
- **(A) 채택**: p03 또는 p10 이 p0 대비 **≥ +0.03** → 더 높은 쪽 (동률이면 작은 값)
- **(C) 0 으로 되돌린다**: 둘 다 **< +0.02** — 이 경우 `pop_boost` 를 **0.0 으로 바꾼다.**
  D-66 의 0.03 은 파일럿 코퍼스의 결론이었으므로, 새 코퍼스에서 재현되지 않으면 유지가 아니라 철회다.
- 사이 → **보류**, 0.03 유지하되 문서에 "미확인" 표시를 남긴다
- **(E) 다양성 안전장치**: 채택 후보의 ILS@50 이 p0 보다 **+0.03 이상 높으면** 채택하지 않는다
- **(F) 저관심 안전장치**: 관심수 하위 25% 시드를 가진 프로필의 합산이 p0 보다 **0.05 이상 낮으면** 채택하지 않는다
- 부수 관측(판정 아님): top-50 관심수 중앙 · 고유 항목 수

## 예상
웹툰 T-2 와 같은 그림을 본다 — **(C) 또는 보류.** 0.03 의 Δ 는 ±0.02 안이고, 배율은 적합률 대신 관심수 중앙을 크게 밀 것이다.
파일럿 코퍼스는 7,062편이라 "인기작"이 이미 과대표집돼 있었고, 29,494편에서는 배율이 더 강하게 작동해 (E)/(F) 에 걸릴 수 있다.
