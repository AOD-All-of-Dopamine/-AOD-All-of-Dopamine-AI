# X-26 사전등록 — TMDB·웹툰 **새로고침 제품 경로(`next_page`) 깊이 검증** (2026-09-15)

## 왜
실제 사용자가 보는 화면은 새로고침(`next_page`)인데, 채점으로 검증된 제품 경로는 **Steam·웹소설뿐이다**(X-20, §71).
TMDB `next_page` 는 2026-09-04 검수에서 새로 만들었고 **한 번도 채점되지 않았다.** 웹툰 `Engine.next_page` 도 채점 기록이 없다.

관측 완료된 기계 사실(채점 없음, 전 프로필 × 5페이지 × 10편, seen 누적 — §100 이후 루프):

| 페이지 | TMDB 시드 코사인 | TMDB 투표 수 중앙 | TMDB 인기 백분위 | 웹툰 시드 코사인 | 웹툰 관심 수 중앙 |
|---|---|---|---|---|---|
| 1 | 0.589 | 457 | 0.837 | 0.540 | 50,087 |
| 3 | 0.568 | 329 | 0.796 | 0.519 | 50,868 |
| 5 | 0.561 | **209** | **0.720** | 0.510 | 53,284 |

장르(TMDB)·태그(웹툰) 시드 피복 중앙은 5페이지까지 1.00. 빈 페이지 0.
**TMDB 는 옛 Steam 과 같은 모양이다** — 유사도는 평평한데(−0.028) 인기가 절반 아래로 떨어진다. 다만 TMDB 에서는 인기 보정이 두 번 기각됐다(D-60 · X-12) — 인기 하락이 곧 품질 하락이라는 보장이 없다. 그래서 채점으로 잰다.

## 질문
> TMDB·웹툰의 실제 새로고침 화면은 5페이지까지 적합률을 유지하는가?

## 설계 (관측 전 고정)
- 프로필: **고정 간격 추출** — TMDB `artifacts/p1/profiles.parquet` 행 순서 `[::4]` = **13개**, 웹툰 `eval/profiles.json` 순서 `[::5]` = **14개**. 점수를 보고 고르지 않는다.
- 경로: 각 플랫폼 현행 PRODUCTION 기본값 `next_page(page_size=10)` 를 5회 연속 호출, 반환 키를 seen 에 누적.
- 채점 대상: 페이지 **1·3·5 전수**(프로필당 30슬롯). 2·4 는 비용 때문에 제외(X-20 과 같다).
- 3역할 새로 채점 · 은행 재사용 없음 · 플랫폼별로 배치를 나눈다(프롬프트가 다르다) · 한 배치 안에서는 페이지·프로필을 섞는다.
- 역할 프롬프트 원문: TMDB `tmdb/eval/v1_preregister.md`(md5 309e7d0d55b09fe6d5ec4fbaad6967cb) · 웹툰 `webtoon/eval/t1_preregister.md`(md5 2f06edaa5549fac17941f79dcebe85e4).
- 시트 meta: TMDB 는 V-4 와 같다(매체 · 장르 · 키워드 · 줄거리), 웹툰은 T-9 와 같다(장르 · 태그 6 · 회차). 인기·평점·페이지 번호는 넣지 않는다.

## 지표
적합 = 3역할 중 2역할 이상 등급 ≥ 2. 페이지별 다수결 적합률 `F1 · F3 · F5`, `하락 = F1 − F5`.

## 판정 (플랫폼별, 관측 전 고정 — 이후 움직이지 않는다)
- **(A) 검증 완료** — `하락 ≤ 0.10` 그리고 `F5 ≥ 0.75` → 변경 없음
- **(B) 경계** — (A)·(C) 가 아닌 경우(`0.10 < 하락 ≤ 0.15` 또는 `0.70 ≤ F5 < 0.75`) → 기록, 깊이 축 후보로 올린다
- **(C) 붕괴** — `하락 > 0.15` 또는 `F5 < 0.70` → 원인을 진단하고(인기 vs 관련성) **별건 사전등록**으로 고친다

참조(판정에 쓰지 않음): X-20 Steam 제품 경로 0.942 → 0.842(−0.100) · 웹소설 0.825 → 0.742(−0.083).

### 부수 관측 (판정 아님)
F3 · 역할별 페이지 곡선 · 페이지 5 에서 인기 백분위 상/하위 절반의 적합률 · TMDB 1페이지가 평가 목록(V-4 h4) top-10 과 같은 프로필 수.

## 예상 (관측 전)
- **TMDB: (A) 또는 (B).** F1 0.88~0.93 · F5 0.78~0.85 · 하락 0.05~0.12. 유사도가 −0.03 밖에 안 떨어지고 장르 피복이 유지되므로 관련성은 버틴다고 본다. 인기 절반 하락이 채점에 얼마나 반영될지가 불확실하다.
- **웹툰: (A).** 하락 ≤ 0.06. 코퍼스가 3,687편으로 작아 5페이지(50편)도 태그 피복 1.00 을 유지한다.
