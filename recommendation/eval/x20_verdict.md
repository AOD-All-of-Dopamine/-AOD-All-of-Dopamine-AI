# X-20 판정 — 제품 경로(`next_page`) 검증: Steam **검증 완료** · 웹소설 **확정값으로 통일**

사전등록 md5 02ef41cdd544296055ec8cb485867adb. 문턱은 관측 전 고정, 이후 변경 없음.
페르소나 v2 앞 12명 · 페이지 1·3·5 전수(팔당 360슬롯) · 신규 783쌍 독립 3역할 만장일치 0.88 · 미채점 0.

## X-20b Steam — 제품 경로 vs 평가 경로

| 팔 | 페이지1 | 페이지3 | 페이지5 | 합산 |
|---|---|---|---|---|
| **제품 경로** (`next_page`) | 0.942 | **0.883** | **0.842** | **0.889** |
| 평가 경로 (`run_multi` 동일 깊이) | 0.950 | 0.867 | 0.808 | 0.875 |
| Δ | −0.008 | +0.016 | +0.034 | **+0.014** |

**판정: (A) 제품 경로 검증 완료.** 문턱 −0.02 이내이고, 오히려 깊은 페이지에서 제품 경로가 낫다(페이지5 +0.034). 추가 장치(`seed_scaled_floor` · `drop_dead_mp` · `series_session_max` · `bucket_offset`)가 해를 끼치지 않으며 꼬리에서 값을 한다.

**이로써 "실제 새로고침 화면이 미검증"이라는 최대 공백이 닫혔다.** 5페이지까지의 하락은 0.942 → 0.842 (−0.100) 로, 문서에 적혀 있던 "3페이지에서 41% 붕괴"(1.79 → 1.06)는 **재현되지 않는다** — `quality_w=0.50`(D-38)이 들어온 뒤로 그 병이 사라진 것으로 보인다.

## X-20a 웹소설 — `max`(현행 제품) vs `top2_mean`(확정값)

| 팔 | 페이지1 | 페이지3 | 페이지5 | 합산 |
|---|---|---|---|---|
| 제품 `max` | 0.783 | 0.767 | 0.708 | 0.753 |
| 확정 `top2_mean` | **0.825** | 0.742 | **0.742** | **0.769** |
| Δ | +0.042 | −0.025 | +0.034 | **+0.017** |

**판정: 차이 없음(+0.017 < +0.02) → 사전등록대로 확정값(`top2_mean`)으로 통일.**
사전등록에 명시한 대로, 이건 새 축 채택이 아니라 **확정값 미적용 결함의 수정**이다(D-55 와 같은 종류). 같으면 확정값이 이긴다. (C) 조건(`max` 가 +0.05 이상 우세)에 해당하지 않으므로 `PRODUCTION["strategy"]` 재검토는 불필요하다.

## 예상 대조
- 웹소설 "`top2_mean` 이 +0.03~+0.07 나을 것, 깊을수록 격차 확대" → **크기는 과대(+0.017), 방향은 맞음.** 다만 페이지3 에서는 역전됐다(−0.025) — 단조 확대가 아니다.
- Steam "±0.02 이내" → **맞음(+0.014).** "`min_reviews` 하한이 저리뷰 프로필을 해칠 수 있다" 우려 → 그런 징후 없음.
- "3페이지 41% 붕괴는 재현되지 않을 것" → **맞음.**

## 함께 고친 것
1. **웹소설 `next_page` 의 `strategy`** — `"max"` 하드코딩 → `PRODUCTION["strategy"]`. (`webnovel/src/personalized_retrieve.py:113`)
2. **Steam 독스트링 오류** — "리뷰 수를 보고하는 게임만(코퍼스의 **39%**)" → 실측 **12.6%** 로 정정. 옛 코퍼스 기준 숫자가 남아 있었다. (`steam/src/personalized_retrieve.py:285`)
3. **웹소설 죽은 줄** — `drop_seed_series` 에서 `keep` 을 계산한 직후 덮어쓰던 줄과 미사용 변수 제거. (`webnovel/src/postprocess.py:112`)

회귀 확인: 웹소설 랭커 3 프로필 top-50 정상(길이 50 · 중복 0).

## 남는 것
- TMDB 에는 `next_page` 가 아예 없다. 제품이 페이지네이션을 요구하면 TMDB 만 못 한다 — 기능 비대칭이고 이번 사전등록 범위 밖이다.
- Steam 의 싫어요 감점 경로(`dislike_weight=2.0`, 유사도 하한 0.45)는 이번 실험에서 시드 없이 돌아 **여전히 미검증**이다. 별도 축.
