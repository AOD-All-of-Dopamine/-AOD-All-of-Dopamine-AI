# X-6 판정 — 채점 신뢰 폭 ±0.03 · 억지 프로필 교체 · **M5 는 판정하지 않았다**

사전등록 `x6_preregister.md` md5 `5b2eb8ea2c8a3a0506a4d2ffbd1a98a5`. 신규 채점 13 → 누적 **623**.

## 1. 0.912 는 얼마나 믿을 만한가 — ±0.03

사용자가 "프로필이 이상한가"를 물었고, 슬롯을 열어 보다 **같은 게임을 같은 조건에서 다르게 매긴 것**을 찾았다
(`Satisfactory`: `x3_coh_cozy` 2 · `x3_thin_bl` 1 — Steam·TMDB 시드가 같고 웹소설만 다르다).
전수로 재니, 동일 랭커 출력을 다른 맥락에서 매긴 **215쌍 중 적합 판정 뒤집힘 10%**, 그중 맥락(clash)으로
설명 안 되는 것 **7쌍 = 3.3%.** ⇒ **통합 P@10 은 잣대 흔들림만으로 ±0.03 움직인다.**
D-72(단일 플랫폼 세션 내 표류)가 통합에도 있다. **외부 앵커 없이는 못 좁힌다.** 이후 모든 통합 수치에 이 폭을 붙인다.

## 2. 프로필 교체 — `x2_sw_horror` → `x2_sw_horror2`

| | 조합 | M3 |
|---|---|---|
| 뺀 것 | seven_horror_coop + **rule_ln**(라이트노벨) | 0.70 |
| 넣은 것 | seven_horror_coop + **rule_hp_mid**(현판) | **0.90** |

라이트노벨 코퍼스 178건, 20화↑ & 관심>0 **15건**. 조합이 아니라 **시드가 설 곳이 없었다.** 내가 만든 억지 조합이었다.
**0.8 미달 4 → 3 은 "개선"이 아니라 정정이다.** 예측(0.8~0.9) 적중.

## 3. 현재 (50 프로필 · M3)

**통합 P@10 0.916 ± 0.03 (잠정, D-25).** 0.8 미만 **3/50**: `x3_clash_2` 0.40 · `x3_clash_1` 0.60 · `x3_thin_bl` 0.70.
M5 +0.014 — **이번엔 판정하지 않는다.** 사전등록대로 신규 프로필 10 이 쌓였을 때 그 10 에서만.

## 남은 0.8 미만 3개의 성격
- `x3_clash_1/2`: 결이 다른 조합. 실패 슬롯의 90% 가 플랫폼 안에서는 적합 — 내가 "이 사람에겐 어색하다"고 판단한 것. 실사용자 없이 못 가른다(X-3). 사용자 "그대로 둔다".
- `x3_thin_bl`: BL 231건(D-23) + 위 3.3% 흔들림(`Satisfactory` 1점이 2점이면 0.80).
**셋 다 규칙·필터로는 닿지 않는다.**
