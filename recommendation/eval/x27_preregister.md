# X-27 사전등록 — TMDB 새로고침 깊이 **전 프로필 재측정** (X-26 (B) 의 크기 확인) (2026-09-15)

## 왜
X-26(md5 4facba27…)에서 TMDB 새로고침 제품 경로가 13프로필(`[::4]`)로 1페이지 0.900 → 5페이지 0.754 (하락 0.146, **(B) 경계**)였다.
같은 X-26 에서 웹툰은 14프로필로 5페이지 0.643 **(C)** 였는데, T-10 대조 팔이 같은 경로를 **67프로필 전부**로 재니 **0.725** 였다(차 0.082, §84 표본 폭의 두 배).
→ 소표본 서술 검증이 깊이 하락을 과장할 수 있다(§102). TMDB 깊이 축을 열기 전에 **전 프로필로 크기를 다시 잰다.**

## 질문
> TMDB 새로고침 1→5페이지 하락은 52프로필 전부로 재도 0.10 을 넘는가?

## 설계 (관측 전 고정)
- 프로필: `tmdb/artifacts/p1/profiles.parquet` **52개 전부**.
- 경로: 현행 PRODUCTION `next_page(page_size=10)` 5회 연속, seen 누적(X-26 과 같은 코드).
- 채점: **1페이지 전수 · 5페이지 전수**(프로필당 20슬롯, 3페이지는 제외 — 크기 확인이 목적). 3역할 새로 채점, X-26 채점 재사용 없음.
- 역할 프롬프트 `tmdb/eval/v1_preregister.md`(md5 309e7d0d55b09fe6d5ec4fbaad6967cb) 원문 · 시트 meta 는 V-4/X-26 과 같다.
- **빌드 assert**: 1페이지 == V-4 h4 top-10 52/52 · 페이지 간 중복 0.

## 판정 (관측 전 고정 — X-26 과 같은 문턱, 이후 움직이지 않는다)
적합 = 3역할 중 2역할 이상 등급 ≥ 2. `하락 = F1 − F5`.
- **(A) 검증 완료** — `하락 ≤ 0.10` 그리고 `F5 ≥ 0.75` → TMDB 깊이 축을 열지 않는다. X-26 (B) 는 소표본 과장으로 기록
- **(B) 경계 확인** — (A)·(C) 가 아닌 경우 → 깊이 축 후보 유지, 원인 진단(인기 vs 관련성)을 **전 프로필 채점으로** 한다
- **(C) 붕괴** — `하락 > 0.15` 또는 `F5 < 0.70` → 원인 진단 후 별건 사전등록

### 부수 관측 (판정 아님)
역할별 F1·F5 · 프로필별 하락 분포 · 5페이지 투표 수 상/하위 절반 적합률 · X-26 13프로필 부분집합만의 F1·F5(같은 프로필, 새 채점 — 채점 잡음 크기 확인용).

## 예상 (관측 전)
**(B), 하락 0.08~0.13.** 웹툰은 67프로필로 늘리자 F5 가 0.08 올랐다. TMDB 도 13프로필 값(0.146)보다 작게 나올 것으로 본다. 다만 투표 수 중앙이 457→209 로 실제로 절반 넘게 떨어지므로 0.10 아래로는 안 내려갈 수 있다.
