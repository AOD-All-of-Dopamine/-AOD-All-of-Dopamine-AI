# X-22 사전등록 — Steam `quality_w` 를 **저리뷰 취향** 프로필에서 별도 판정 (2026-08-31)

## 왜
`quality_w=0.50` 은 52프로필 전체 평균으로 확정됐다(0.464 → 0.769, D-38·D-50/51). 그런데 PLATFORM_AUDIT 실측이 보여준 것은 이 항이 **미세 조정이 아니라 사실상 인기 필터**라는 것이다:
- 보정을 끄면 **top-50 의 73%가 바뀐다**(겹침 26.8%), 리뷰 수 중앙 10,657 → 0.
- 코퍼스의 87.4%가 리뷰 미보고인데 추천 목록에는 0.1%만 들어온다 → **실질 카탈로그 12.6%**.

즉 **"취향 적합"과 "안전한 유명작"이 이 축에서 분리되지 않았다.** TMDB 는 같은 문제를 D-31 에서 겪고 인기 부스트를 지웠다(대작 취향엔 좋고 롱테일엔 해로웠다). Steam 에서 그 대칭 검사를 한 적이 없다.

## 대상군 — 저리뷰 취향 9개 (등급을 보지 않고 정의된다)
`steam/artifacts/p1/profiles.parquet` 에서 **시드 리뷰 수 중앙값이 1,500 미만**인 프로필 전부:
`longtail_deckbuilder`(288) · `longtail_metroidvania`(291) · `longtail_detective`(296) · `longtail_puzzle_platformer`(296) · `lowrev_towerdefense`(865) · `lowrev_deckbuilder`(868) · `lowrev_cozy_narrative`(896) · `lowrev_metroidvania`(897) · `lowrev_detective`(1,238).
대조군으로 **대작 취향 상위 5개**(시드 중앙 최대: `coh_fps` 1,762,199 · `coh_classic_multi` 808,644 · `mix2_coop_horror` 410,806 · 그다음 2개)도 같이 잰다 — 축이 취향에 따라 부호가 갈리는지 보기 위해서다.
경계값 1,500 은 관측된 시드 중앙값 분포(288~1,238 다음이 1,902)에서 자연스러운 틈이고, **등급과 무관하게 정해졌다.**

## 변형
`quality_w ∈ {0.00, 0.25, 0.50(현행)}`. 다른 확정값(`tag_w` 0.40 · `mc_w` 0.20 · `rec_boost` 0.03 · `hub_lambda` 0.35 · top2_mean)과 후처리는 **불변**. 한 축만 움직인다.

## 측정
각 변형의 Steam **단독** top-50. 1-10 전수 + 11-50 표본(프로필당 10). 기존 채점 은행 재사용, 신규만 독립 3역할 눈가림 다수결.

## 판정 (관측 전 고정)
저리뷰 9개에서:
- **(A) 저리뷰용 값 분리**: `quality_w` 0.00 또는 0.25 가 현행(0.50) 대비 합산 **≥ +0.05** → 취향에 따라 값을 나눠야 한다는 근거. 어느 값이 이겼는지와 함께 기록하고, **서빙 분기 도입은 별도 사전등록**으로 다룬다(이번엔 값을 바꾸지 않는다).
- **(C) 현행 확정**: 0.50 이 두 대안보다 모두 높거나 차이가 +0.02 미만 → `quality_w=0.50` 이 저리뷰 취향에서도 옳다. 축 종료.
- 사이(+0.02~+0.05) → 보류.
대조군(대작 5개)은 **판정에 쓰지 않는다.** 부호가 갈리는지 보는 관찰용이다.

## 예상
- 저리뷰 9개에서 `quality_w=0.00` 이 현행보다 **+0.05~+0.12** 나을 것으로 본다. 이 프로필들의 시드 자체가 리뷰 300~1,200 대인데, 현행은 리뷰 1만 대를 데려온다 — 취향과 어긋난다.
- 0.25 는 그 중간(+0.02~+0.06).
- 대조군에서는 반대로 0.50 이 가장 좋을 것 — D-31 이 TMDB 에서 본 그림과 같다.
- **다만 이 채점으로는 결론이 안 날 수도 있다.** 채점자(LLM)가 유명작을 선호한다면 0.50 이 이기는데, 그건 "취향 적합"이 아니라 잣대의 성질일 수 있다. 그 경우에도 **문턱대로 판정하고**, 한계를 판정문에 명시한다. 이 축의 진짜 답은 사람 채점(시험대 "볼 의향")에서 나온다.
